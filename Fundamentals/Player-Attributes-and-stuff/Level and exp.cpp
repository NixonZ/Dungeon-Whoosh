#include<iostream>
using namespace std;
void exp()
{
    int exp=0,level=0;
    if(level>=5)
    {
        level=5;
    }
    else if(exp=level*10+10)
    {
        exp=0;
        level++;
    }
}
